import React from 'react';

/**
 * Dedicated component for any floating decorative elements in the Hero section.
 * Currently empty, ready for SVG or small floating UI components.
 */
export default function HeroDecorations() {
  return (
    <div className="pointer-events-none absolute inset-0 z-20">
      {/* Add floating UI elements or additional decorative SVGs here later */}
    </div>
  );
}
